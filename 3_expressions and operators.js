// 1:-
// Assignment operator

// an assignment operator assigns a value to its left operand.
// based on the value of its right operand.
// the simple assignment operator is =

//var x = 5;
//var y = 5;

//console.log("is both are equal or not " + x == y);
// console.log('is both are equal or not ' ${x == y});


// 2:-
// Arithmetic operators

// an arithmetic operators takes the numerical values(either literals or variables) 
// as their operands and returns a single numerical values.

// console.log(3+3);
// console.log(10-4);
// console.log(10*4);
// console.log(100/10);
// console.log("remainder operator" + 10%4);




// 3:-
// Increment and decrement operator
// increment operator
// if used postfix with operator after operand(for eg X++)
// the increment operator increments and returns the value before incrementing.


// decrement operator
// if used prefix with operator before operand(eg ++X)
// the increment operator increments and return the value after incrementing.

//var num= 15;
//var newnum=num++;
//console.log(num);
//console.log(num++);
//console.log(newnum);





// Postfix increment operator means the expression is evaluated.
// first using the original value of the variable and then the variable is incremented.
// If used Prefix with operator before operand( ++x)
// the increment operator increments and return s the value after incrementing.

//var num=15;
//var newnum=++num;
//console.log(num);
//console.log(newnum);

// postfix increment operator means the variable is incremented first and then the expression is evaluated using the new value of the variable.




// 5:-
// Comparison Operator

// A comparison operator compares its operands and returns a logical value based on whether the comparison is true or false.

 //var a=10;
 //var b=4;
 //console.log(a==b);
//console.log(a!==b);
 //console.log(a > b);

// 6:-
// LOGICAL OPERATOR
// Logical operators are typically used with boolean logical values when they are return a boolean value.
//var a= 30;
//var b= -20; 

// Logical And(&&):- The logical And(&&)operator for a set of operands is true and only if all of its operands are true.
// console.log(a>b && b>0)

// Logical OR(||):- The logical OR (logical disjunction) for a set of operands is true if and only if one or more of its operand is true.
// console.log(a>b || b>0)

// Logical NOT
// logical not operator(logical complement negation )takes truth to falsity and vice versa.
// console.log(! false)


// 7:-
// STRING OPERATOR
// Concatenation operator
// The operator(+)concatenates two strings values together,returning another string that is the union of the two operands strings.

// console.log("hello" + " World");
//var name ="Nitin";
//console.log(name +" Shukla");




